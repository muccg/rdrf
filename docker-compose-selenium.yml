seleniumhub:
  image: selenium/hub
  environment:
    - GRID_TIMEOUT=120000
  log_driver: json-file
  log_opt:
    max-size: "10m"
    max-file: "5"
  ports:
    - "4444"

chrome:
  image: selenium/node-chrome
  environment:
    - SCREEN_WIDTH=3520
    - SCREEN_HEIGHT=1200
    - SCREEN_DEPTH=16
  links:
  - seleniumhub:hub

firefox:
  image: selenium/node-firefox
  environment:
    - SCREEN_WIDTH=3520
    - SCREEN_HEIGHT=1200
    - SCREEN_DEPTH=16
  links:
  - seleniumhub:hub
