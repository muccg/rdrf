seleniumhub:
  image: selenium/hub
  log_driver: json-file
  log_opt:
    max-size: "10m"
    max-file: "5"
  ports:
    - "4444"

chrome:
  image: selenium/node-chrome
  environment:
    - SCREEN_WIDTH=1240
    - SCREEN_HEIGHT=1024
    - SCREEN_DEPTH=16
  links:
  - seleniumhub:hub

firefox:
  image: selenium/node-firefox
  environment:
    - SCREEN_WIDTH=1240
    - SCREEN_HEIGHT=1024
    - SCREEN_DEPTH=16
  links:
  - seleniumhub:hub
